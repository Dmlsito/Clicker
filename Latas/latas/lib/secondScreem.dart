import 'package:flutter/material.dart';

class secondScreem extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
   return Scaffold(body: Container(child: Text("Esta es una pantlla de prueba")));
  }
  
}