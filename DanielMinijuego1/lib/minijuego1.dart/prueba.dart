import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

class Prueba extends StatelessWidget {
  @override
  Widget build(Object context) {
    return Scaffold(body: Center(child: Text("HOla que tal")));
  }
}
